<!DOCTYPE html>
<html lang="es">
<head>
    <meta charset="UTF-8">
    <title>{% block title %}Administración{% endblock %}</title>
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css" rel="stylesheet">
    <style>
        body { background: #f5f5f5; color: #222; }
        .sidebar { background: #1a1a1a; color: #fff; min-height: 100vh; }
        .sidebar a { color: #ccc; display: block; padding: 10px; text-decoration: none; }
        .sidebar a:hover { background: #333; color: #fff; }
        .content { padding: 20px; }
        .table th, .table td { vertical-align: middle; }
        .btn-primary { background: #000; border: none; }
        .btn-primary:hover { background: #444; }
        .btn-danger { background: #222; border: none; }
        .btn-danger:hover { background: #555; }
    </style>
</head>
<body>
<div class="d-flex">
    <div class="sidebar p-3">
        <h2>Admin</h2>
        <a href="{{ path('admin_dashboard') }}">Dashboard</a>
        <a href="{{ path('admin_pedidos_list') }}">Pedidos</a>
        <a href="{{ path('admin_productos_list') }}">Productos</a>
        <a href="{{ path('admin_usuarios_list') }}">Usuarios</a>
        <a href="{{ path('admin_estadisticas') }}">Estadísticas</a>
    </div>
    <div class="content flex-grow-1">
        {% for message in app.flashes('success') %}
            <div class="alert alert-success">{{ message }}</div>
        {% endfor %}
        {% block body %}{% endblock %}
    </div>
</div>
</body>
</html>
